#include <Arduino.h>

void setup() {
  Serial.begin(9600);
  pinMode(26, INPUT_PULLUP); // same as your main code
}

void loop() {
  bool state = digitalRead(26);
  Serial.println(state ? "NOT PRESSED" : "PRESSED");
  delay(200);
}